#include <stdio.h>
#include <string.h>
int main ()
{
	char nombre[] = "<define>";
  char input[20];
  do {
     printf ("Escriba <define> para comenzar \n");
     gets (input);
  } while (strcmp (szKey,szInput) != 0);
  puts ("Correct answer!");
  return 0;
	
}

